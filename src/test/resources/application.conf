##
## jdbc donnection related attributes
##
vevo {
  db {
    jdbc {
      url = "jdbc:h2:mem:qos"
      user = "user"
      password = "secrete-password"
      driver = "org.h2.Driver"
    }
    connection.pool {
      threads = 5
    }
  }
}